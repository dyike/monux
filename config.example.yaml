monitor:
  # Find this number with: monux detect
  bus: 15

inputs:
  # Labels identify the computers connected to these monitor inputs.
  # These are common Dell P2415Q values; confirm them for your cabling/display.
  mac: 0x11
  linux: 0x0f
